# Scripts

This directory is intentionally empty of executable scripts until the app has a
real first vertical slice.

The first script should be `smoke-test.mjs`, checking a health endpoint and the
seeded Kitchen Sound Detectives flow without requiring a live model request.
