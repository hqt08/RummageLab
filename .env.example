# Optional server-only development key for live analysis. Never expose this to the browser.
OPENAI_API_KEY=

# The starter runs without a database. Add a server-only database URL later.
DATABASE_URL=

NEXT_PUBLIC_APP_URL=http://localhost:3000
DEMO_MODE=true
