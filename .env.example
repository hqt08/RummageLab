# Set only when live OpenAI integration begins. Never expose this to the browser.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6

# The starter runs without a database. Add a server-only database URL later.
DATABASE_URL=

NEXT_PUBLIC_APP_URL=http://localhost:3000
DEMO_MODE=true
