# Components

Reserved for presentational and interactive components.

First implementation slice, after approval:

1. `QuestContextForm`
2. `RummageToolRenderer`
3. a single `PredictTool`

Do not create the five activity tools at once. Prove the end-to-end quest flow
with one tool before expanding the renderer.
