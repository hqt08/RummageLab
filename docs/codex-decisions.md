# Codex decision log

Use this as evidence of the human/Codex collaboration. Keep entries brief,
dated, and honest. The README remains the submission-facing summary.

| Date | Decision | Why | Human decision | Codex contribution |
| --- | --- | --- | --- | --- |
| 2026-07-15 | Build a web-first PWA | Fast public testing and mobile-friendly judging | Approved the product surface | Proposed and scaffolded the architecture |
| 2026-07-15 | Scope the live demo to a parent and 3-year-old | A real family can demonstrate co-play, language, adaptation, and safety | Confirmed | Reframed the age-banded architecture and demo target |
| 2026-07-15 | Use Kitchen Sound Detectives as the demo activity | It uses safe, familiar, large household materials and supports sound, pattern, language, and turn-taking play | Confirmed | Proposed the activity and parent-reflection loop |
| 2026-07-15 | Make an object photo visible in the 3-year-old demo | Demonstrates GPT-5.6 multimodal reasoning in a concrete parent workflow | Confirmed | Defined photo inventory, parent-confirmation, seeded fallback, and demo flow |
| 2026-07-15 | Use a seeded, no-login parent context | Reliability and minimal child-data handling matter more than account features in the demo | Confirmed | Documented a no-persistence architecture boundary |
| 2026-07-15 | Make parent reflection optional, asynchronous, and typed-or-voice | The adaptive loop should be useful without recording a child or forcing disclosure | Confirmed | Defined transient reflection and parent-approved context boundaries |
| 2026-07-15 | Keep preference memory out of the hackathon | The demo proves adaptation without retaining child-related data | Confirmed | Defined future parent-owned, tag-only, expiring preference boundary |
| 2026-07-15 | Use `RummageTool` in product-facing language | Clear, durable name for the approved learner interaction | Confirmed | Defined the safe renderer boundary |
| 2026-07-15 | Keep voice reflection asynchronous | Reliable demo latency and lower implementation risk | Confirmed, with typed input and skipping also available | Recommended and documented the flow |
| 2026-07-15 | Stop at scaffold/framework level | Preserve user approval before any learner-facing implementation | Requested explicitly | Replaced the starter visual demo with a neutral scaffold placeholder |
| 2026-07-16 | Use Anchorage as the editable demo city and let live weather suggest tags | Familiar rainy-day context with fast setup while preserving parent judgment and a manual fallback | Confirmed Anchorage and mandatory parent approval | Compared open providers and defined a deterministic, parent-approved adapter boundary |
| 2026-07-16 | Publish source at `hqt08/RummageLab` and deploy the app with Vercel | Gives judges direct access, PR previews, and a local fallback while supporting server routes | Confirmed public GitHub source and Vercel hosting | Audited publish readiness and defined seeded previews plus `main` production deployment |
| 2026-07-16 | Introduce worktrees only after the baseline commit | Parallel slices help later, but an all-untracked repository has no stable integration point | Asked for a worktree recommendation | Defined one-task-per-worktree and Local integration boundaries from the Codex worktree guidance |
| 2026-07-16 | License RummageLab code and documentation under Apache-2.0 | Meets the public-repository requirement with a permissive license and an explicit patent grant while reserving trademark rights | Selected Apache-2.0 after reviewing alternatives and the official rules | Compared permissive and copyleft choices and added standard license metadata |
| 2026-07-16 | Use `hqt08` and a GitHub no-reply commit address for the hackathon | Keeps the public repository tied to the entrant account without exposing a personal email | Confirmed `hqt08` now, with a possible later LLC transition | Resolved the account's ID-based no-reply address and scoped the Git configuration to this repository |
| 2026-07-16 | Adopt the GitHub-generated Apache license commit as the `main` baseline | Avoids unrelated histories while preserving the reviewed local scaffold for a later intentional commit | Created the public repository with Apache-2.0 selected | Verified the remote author identity and byte-identical license, fetched the commit, and attached local `main` without committing or pushing |
| 2026-07-16 | Keep the in-product Codex Studio in phase two | The hackathon is stronger with one polished, safe, low-latency learner loop than a second authoring product | Confirmed phase-two scope | Separated Codex's material build-time role from GPT-5.6 runtime selection of validated, prebuilt RummageTools |
| 2026-07-16 | Pin the scaffold to Node 24, pnpm 9.15.9, and patched dependencies | Node 20 is EOL, Vercel defaults to Node 24, and the initial dependency graph contained published Next.js and PostCSS advisories | Requested mechanical scaffold readiness | Updated the compatible dependency line, added a documented PostCSS override, generated the lockfile, and verified lint, types, scaffold tests, build, audit, smoke, and staged-secret checks |
| 2026-07-16 | Establish the reviewed scaffold baseline on `main` before product work | Gives every implementation slice a clean, auditable integration point | Explicitly authorized the reviewed commit and push | Rechecked the staged scope and full check, committed `aab2556`, pushed it, and verified a clean synchronized `main` |
| 2026-07-16 | Implement the first golden path as an honest seeded, session-only slice | Judges need a polished path without credentials, while live integrations should not be simulated or widen data collection | Bounded the slice to Kitchen Sound Detectives and prohibited pushing, deployment, and live APIs | Built the Zod-parsed fixture and reducer, approved `sound_mix` renderer, prepared observation branch, tag-only one-suggestion boundary, original local demo media, focused tests, and phone/desktop QA |
| 2026-07-16 | Merge the seeded golden path through pull request #1 | Establishes a reviewed feature baseline before integrations | Approved the orchestrated branch and PR workflow | Published, reviewed, and merged `feat/kitchen-sound-demo`, then verified local and remote `main` at `041cf34` |
| 2026-07-16 | Add a local photo and typed-material intake shell before live multimodal analysis | Proves the parent-facing intake and confirmation boundary without collecting data or simulating GPT output | Previously confirmed that photo and typed input must converge on parent approval | Added browser-local photo preview, deterministic typed normalization, stale-confirmation invalidation, tests, and responsive browser QA with no network or persistence |
| 2026-07-16 | Use one RummageLab-scoped Codex task per implementation worktree | Keeps parallel work auditable and prevents an orchestration conversation from becoming an implicit shared checkout | Asked Codex to remain the orchestrator and verify task/worktree isolation | Audited all three worktrees, confirmed `main` remained clean, documented the current manual-worktree exception, and bounded later slices to managed project worktrees |
| 2026-07-16 | Add least-privilege GitHub Actions CI | Keeps the seeded demo's reproducible quality gate visible on every pull request and `main` update without granting credentials or deployment access | Requested CI readiness limited to validation | Added Node 24 and pinned pnpm 9.15.9 checks with frozen install, dependency-store caching, PR-only cancellation, immutable action revisions, and read-only contents permission |
| 2026-07-16 | Prepare the runtime contract before live GPT-5.6 integration | A real photo-to-activity vertical slice needs strict server validation and a useful no-key fallback without prematurely handling child-related content | Authorized only a future server-only development key and transient object-only upload boundary; deferred PWA, live weather, typed reflection, voice, routes, SDK, outbound calls, persistence, analytics, and deployment | Added provider-neutral request/response contracts, deterministic seeded provider, compatibility checks, content-free failure taxonomy, automatic fallback/retry UI, and coverage without a live adapter |
| 2026-07-16 | Add the focused live GPT-5.6 photo-to-activity slice behind a no-key-safe capability gate | The flagship demo needs the object photo and confirmed context to visibly influence a real, constrained activity without sacrificing the judge-ready seeded path | Authorized a server-only development key and transient object-only uploads for this slice; production credentials, deployment, PWA, weather, voice, and reflection remain separate | Added in-memory image re-encoding, stateless structured Responses API calls, strict Zod and context validation, content-free failures, seeded fallback, reset behavior, tests, and honest setup/privacy documentation |
| 2026-07-16 | Add optional typed parent reflection behind a deterministic privacy guard and explicit approval gate | The adaptive loop should accept a useful parent observation without creating a child profile, retaining free text, or making reflection mandatory | Authorized only short typed parent input, transient server-only extraction, and a prepared no-key fallback; voice/transcription, persistence, analytics, authentication, PWA, live weather, and deployment remain deferred | Added shared conservative client/server PII-risk screening, strict byte/character and Zod boundaries, stateless `store: false` structured extraction, safe fallback/retry and stale-response handling, editable draft review, and a separate parent-approved tag-only context for one next idea |
| 2026-07-16 | Merge the runtime contract, live-photo, and typed-reflection implementation slices behind the seeded fallback | The public demo must remain complete without a key, while the source must prove a real GPT-5.6 integration with server-side validation and privacy boundaries | Approved the lean route: live GPT path in source, no PWA or live weather, and typed reflection before voice | Orchestrated focused worktrees and reviews; merged PRs #5–#7 after Node 24 CI, production browser QA, privacy review, cancellation tests, and secret/dependency checks |
| 2026-07-17 | Deploy the public Vercel demo with no production model key | A judge-ready public app should not expose an unrate-limited live model path; the deterministic no-key experience is complete and verifiable | Signed in to Vercel, imported `hqt08/RummageLab`, and approved a no-environment-variable production deployment | Verified <https://rummage-lab.vercel.app/> in a fresh browser through safety confirmation, seeded activity, tag approval, one next idea, and reset; recorded the URL in the README and submission checklist |
| 2026-07-17 | Make submission evidence explicit rather than claiming unfinished assets | The official submission requires a public demo, English materials, a public YouTube video under three minutes with audio, source access, and a real core `/feedback` Session ID | Retained the no-key public demo and deferred only owner-produced video, Devpost form, and real feedback evidence | Cross-checked the official rules, updated the checklist, and merged CI-verified documentation PRs #8–#10 without fabricating a Session ID or video URL |
| 2026-07-17 | Default live OpenAI work off behind a server-only Vercel switch | A public demo needs an immediate manual emergency stop that prevents uploads and model work while preserving the seeded path; this is not a billing cap | Authorized a fail-closed `RUMMAGELAB_LIVE_OPENAI_ENABLED` gate and owner-only operating guidance; production variable changes remain manual | Added the capability gate before parsing/provider creation, closed diagnostics, no-upload photo UI, tests, and documentation that a $10 hard limit cannot be guaranteed |
| 2026-07-17 | Extend the optional live intake to bounded typed object labels | Parents should be able to start from ordinary named objects as well as photos, without treating free text as context or exposing it in telemetry | Requested dynamic object suggestions with the same parent-confirmation boundary | Added a 1–5 label, 80-character, client/server PII-and-hazard prefilter; stateless structured category mapping; safe fallback; and HTTP-status/request-ID-only provider diagnostics |
| 2026-07-17 | Let GPT-5.6 select only reviewed activity template IDs after parent confirmation | A photo or typed object should influence a useful activity without executing or displaying model-authored learner software | Requested a dynamic activity for approved everyday objects, with Kitchen Sound retained for its exact kit | Added a bounded Kitchen/ball/generic template selector, a prebuilt non-recording `predict` renderer, and local validated fallback text; model output is an ID only and is rechecked against age, time, setting, and confirmed categories |
| 2026-07-17 | Open object intake to any GPT-vetted object behind a local hard denylist and parent final approval (Slice 1) | Showcasing adaptive live GPT requires more than a fixed 8-object enum; the parent (not a static allowlist) should be the final gate, with a fail-closed floor beneath | Approved "open + hard denylist": any object may be surfaced with warnings, a local denylist auto-rejects clear hazards, and the parent confirms each; seeded path retained | Added an `other_safe_object` category, per-object `safetyLevel`/`warnings`, a shared client+server hard denylist that filters model labels before a parent sees them, label-based dedup, a Zod-derived model enum (no hand copy), and surfaced warnings in the confirm UI |
| 2026-07-17 | Let GPT-5.6 author the full activity for live intake, re-validated against the parent-approved context (Slice 2) | The flagship "adaptive" claim is stronger when age + weather + confirmed objects yield a bespoke activity, provided nothing model-authored reaches a child unvalidated | Approved the thesis shift from "model output is an ID only" to "model authors, but three gates hold": local hard denylist, strict Zod + context invariants, and parent review before play; seeded kit stays deterministic | Added a strict full-QuestSpec generation schema (enum-constrained materials/focus IDs, five approved tools), server re-validation (age, materials subset, catalogue, time window), a per-object identity model so multiple open objects flow with labels, three new non-recording renderers, and an always-available reviewed fallback |
| 2026-07-17 | Surface activity provenance and a generated summary, and keep the seeded golden path deterministic (Slice 3) | Judges and parents should see whether an activity was generated live or prepared, read a one-line synopsis, and still get the reliable no-latency Kitchen Sound path | Approved the honest live/prepared/fallback badge, the generated `activitySummary`, object-neutral copy, and a 45s generation timeout with a fail-to-fallback path | Added the provenance badge and summary card, neutral activity copy, a longer generation timeout with `maxDuration`, and a regression test asserting the seeded prepared kit makes no model call |
| 2026-07-17 | Lead the demo with age-stage focus and reveal Kitchen Sound Detectives only after its full kit is confirmed | The product should make its 0–6 developmental scope visible without presenting a 3–4 activity, duration, or setting before the parent has established a fitting kit | Requested age-band categories and delayed activity reveal | Added catalog-aligned parent-facing age choices, explicit current-demo scope for non-3–4 bands, state reset on age changes, and an exact-kit activity-unlock card shared by seeded, photo, and typed intake |
| 2026-07-17 | Fail closed when live photo inventory analysis is unavailable or invalid | A parent must never be shown seeded Kitchen objects as if they came from an unrelated photo | Approved a focused P1 bugfix before broader adaptive expansion | Replaced the unrelated seeded-photo fallback with a content-free retryable error, cleared stale browser candidates, and added provider-failure and malformed-output regression coverage |
| 2026-07-18 | Extend the adaptive flow to every age band (0–1, 1–2, 3–4, 5–6) | The adaptive claim should hold across the product's whole age range, while under-threes stay caregiver-led, screen-free, and on the smaller material allowlist | Approved per-band generation with the same three gates (denylist, Zod + context re-validation, parent confirmation), reviewed per-band fallbacks, and an unchanged 3–4 seeded golden path | Added per-band reviewed fallback moments/quests built from the confirmed context, band-aware vetting (under-three enum + conservative prompt), a RummageMoment generation schema for 0–2 and a parametrized quest schema for 3–6, age pass-through in the route/multipart, under-three confirmation guards in the reducer and UI, a caregiver-script screen, and per-band deterministic seeded kits |
| 2026-07-18 | Default live GPT-5.6 activity authoring to low configurable reasoning | The bespoke activity/moment request is the latency-sensitive live step; object inventory should remain as lean as possible | Requested a configurable `reasoning: { effort: "low" }` setting while retaining the GPT-5.6 default | Added a server-only validated `RUMMAGELAB_OPENAI_REASONING_EFFORT` setting (default `low`), applied it only to generated activities/moments, and covered default/override plus seeded regressions |
| 2026-07-19 | Broaden the observation-tag vocabulary with activity-neutral tags | After generated (non-sound) activities, feedback had to shoehorn into the Kitchen-Sound-era tags, weakening the accuracy of the tag-only boundary | Approved six neutral additions (stacking, hiding/finding, counting, pretend play, balancing, watching/waiting) while keeping the closed allowlist and parent approval unchanged | Added the tags to the Zod source of truth, derived the reflection model enum from it (removing a hand copy), extended UI labels and local fallback phrases, and covered the vocabulary with sync tests |
| 2026-07-19 | Add per-IP rate limiting to all billable live operations | A public demo with live GPT enabled needs a cost floor against scripted abuse; Vercel WAF rate rules require a paid plan | Approved an in-code sliding-window limiter (default 20/hour per IP, env-tunable), seeded paths exempt, content-free 429s, and honest per-instance-memory scoping documented as an abuse floor rather than a billing control | Added the shared limiter with bounded memory and no IP logging, wired both API routes before provider work, graceful client fallback messages, README operating documentation, and limiter/route tests |
| 2026-07-19 | Author the try-next idea live after generated activities, keeping the golden-path reflection intact | After a bespoke activity, a canned kitchen-themed idea breaks the adaptive story; the parent-reviewed note plus approved tags are the right, already-guarded inputs | Approved sending only the parent-reviewed summary, approved tags, confirmed object labels, age band, and weather tags to a live author; Kitchen Sound keeps its prepared example and deterministic suggestion; Skip unchanged | Added a strict next_suggestion contract and provider, server-side fallback to a local tag-only idea, the same parent-approval reducer gate for applied ideas, honest live/prepared provenance on the final card, and contract/provider/state tests |
| 2026-07-19 | Add a curated city picker and a live Open-Meteo weather lookup that suggests tags | The weather context becomes genuinely adaptive without exposing family location or adding billable model calls; a lookup table maps a small closed tag set exactly | Approved a curated public-city dropdown, a keyless client-side Open-Meteo lookup of fixed public coordinates, and deterministic on-device tag mapping with mandatory parent approval | Added the curated city list, the WMO-code/temperature/wind tag mapper with bounded output, a weather_lookup source threaded into the activity context, reapproval-forcing reducer action, spinner/status UI, and mapping/fetch/state tests |
| 2026-07-19 | Enable the live GPT-5.6 path on the public Vercel deployment for the judging window | Judges should be able to experience live photo-to-activity authoring first-hand; the provenance badges and fail-closed seeded fallback keep the demo complete if credits run out | Added `OPENAI_API_KEY` and `RUMMAGELAB_LIVE_OPENAI_ENABLED=true` as server-side Production variables per the live-mode operating boundary (dedicated project, locked limits, prepaid balance, auto-recharge off), keeping the switch as the manual emergency off | None at runtime; the previously built capability gate, provenance badges, and seeded fallback are what make this toggle safe to flip |
| 2026-07-21 | Keep a guarded typed parent note visible through immediate review, with GPT-derived tags | The parent needs to recognize the note they just wrote, while the live model should supply only constrained tag suggestions and the Kitchen Sound golden path must stay deterministic | Requested the actual typed note in Case 3, live GPT tag extraction, and retention of Skip plus the prepared demo route | Kept the note browser-session-only and out of server responses, threaded the configurable low-reasoning model settings into reflection/next-idea requests, and added live/seeded/skip regressions |

## Core-session evidence

- **Core Codex `/feedback` Session ID:** `019f6671-c7e4-7cc1-af9a-1bc7b21370af`
- **Local repository created:** `2026-07-15`
- **Public GitHub repository created:** `2026-07-16`
- **Important commits:** [`8615945`](https://github.com/hqt08/RummageLab/commit/861594519773fcc574fc73a3c462e4707a698866) — GitHub-generated Apache-2.0 license; [`aab2556`](https://github.com/hqt08/RummageLab/commit/aab2556356de3802f12af535b868fc4618002788) — reviewed scaffold baseline; [`041cf34`](https://github.com/hqt08/RummageLab/commit/041cf3402c3ca3bfbd8e70335180f7e703bd24ec) — merged seeded Kitchen Sound demo; [`257059a`](https://github.com/hqt08/RummageLab/commit/257059a) — runtime contracts and seeded fallback; [`d3b5db6`](https://github.com/hqt08/RummageLab/commit/d3b5db6) — live GPT-5.6 photo experience; [`6ab635a`](https://github.com/hqt08/RummageLab/commit/6ab635a) — typed parent reflection; [`8bcc449`](https://github.com/hqt08/RummageLab/commit/8bcc449) — verified public deployment record; [`2e17f1e`](https://github.com/hqt08/RummageLab/commit/2e17f1e) — official submission requirements

## Entry template

```md
| YYYY-MM-DD | Decision | Why | Human decision | Codex contribution |
```
